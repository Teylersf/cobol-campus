import Link from "next/link"; import {lessons} from "@/lib/content";
export const metadata={title:"Complete COBOL course"};
export default function Course(){const tracks=[...new Set(lessons.map(l=>l.track))];return <div className="catalog-page"><header className="topbar"><Link href="/" className="brand"><span className="brand-mark">C</span><span>COBOL.campus</span></Link><nav><Link href="/course">Course</Link><Link href="/tools">Tools</Link><Link href="/#projects">Projects</Link></nav><Link className="nav-cta" href="/learn/getting-started">Start course →</Link></header><main className="catalog-main"><span className="kicker">22 LESSONS · 5 TRACKS · FREE FOREVER</span><h1>The complete<br/>COBOL course.</h1><p className="lead">A practical path from your first compiled program to files, DB2, CICS, testing, and modernization.</p><div className="course-tracks">{tracks.map((track,ti)=><section key={track}><div className="track-label"><span>0{ti+1}</span><h2>{track}</h2><small>{lessons.filter(l=>l.track===track).length} LESSONS</small></div><div className="lesson-cards">{lessons.filter(l=>l.track===track).map(l=><Link href={`/learn/${l.slug}`} key={l.slug}><span>{String(l.number).padStart(2,"0")}</span><div><b>{l.title}</b><p>{l.summary}</p></div><small>{l.duration}</small><i>→</i></Link>)}</div></section>)}</div></main></div>}
